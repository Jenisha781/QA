name: Hello World with Steps
on:
  push:
    branches:
      - main
jobs:
  hello-world-job:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v2
      - name: Print Hello World
        run: echo "Hello World"
      - name: Print Date and Time
        run: date
